# 配布物の更新

人がこのリポジトリの`main`を変更するときは、作業ブランチからPull Requestを作ります。

正式版のインストーラは、非公開の開発リポジトリで検査を通した同じ組み立て結果から公開します。発行専用のGitHub Appは、Releaseの作成と添付だけを行います。apt配布元は`gh-pages`へ生成し直すため、この二つの自動更新に限ってPull Requestを経由しません。

ソースコード、開発途中の配布物、秘密鍵は置きません。

